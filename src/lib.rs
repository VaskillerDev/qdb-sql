#[macro_use]
extern crate log;
/// The module which describes all abstract syntax tree
/// (types, notations and etc.).
pub mod ast;
/// The module for parsing string-based instances into an abstract syntax tree.
pub mod parser;
